<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Banter Box</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white h-screen flex flex-col justify-between">
    <!-- Header -->
    <header class="bg-gray-800 text-white text-center py-4 shadow-lg">
        <h1 class="text-3xl font-extrabold">Banter Box</h1>
    </header>

    <!-- Chat Container -->
    <main id="main" class="flex-1 overflow-y-auto p-6 space-y-4">
        <!-- Incoming Message -->
        <!-- <div class="flex flex-col items-start recieved">
            <span class="text-sm text-gray-400">User 1</span>
            <div class="bg-gray-700 rounded-lg shadow-lg p-4 max-w-xs text-white">
                <p>Hello! How are you?</p>
            </div>
        </div> -->

        <!-- Outgoing Message -->

        <!-- <div class="flex flex-col items-end sent">
            <span class="text-sm text-gray-400">You</span>
            <div class="bg-gray-500 text-white rounded-lg shadow-lg p-4 max-w-xs">
                <p>Hello! How are you?</p>
            </div>
        </div> -->

      
    </main>

    <!-- Input Section -->
    <footer class="bg-gray-800 p-4 flex items-center space-x-4 shadow-lg">
        <input id="myinput" type="text" placeholder="Type a message" class="flex-1 bg-gray-700 border border-gray-600 rounded-full p-3 text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-gray-500">
        <input id="mybutton" type="submit" class="bg-gray-600 text-white rounded-full px-6 py-3 hover:bg-gray-700 transition duration-300 ease-in-out transform hover:scale-105" value="Send"></input>
    </footer>

    <script src="/socket.io/socket.io.js"></script>
<script>
  const socket = io();

  let user;
  do{
   user= prompt("enter your name")
  }
  while(!user);

  const myinput= document.querySelector("#myinput");
  const mybutton= document.querySelector("#mybutton");
  let maindiv=document.querySelector("#main");
  

  //
  mybutton.addEventListener("click",function(){

      sendMessage(myinput.value,user,"sent");
  })


  // sendMessage function

 function  sendMessage(message,user,type){

    appendMesaage(message,user,type);

  }


  function appendoutgoingMesaage(message,user,type){
    let maindiv=document.querySelector("#main");

    const newDiv = document.createElement("div");


    let elem= `
         <div class="flex flex-col items-end sent">
            <span class="text-sm text-blue-400">@${user}</span>
            <div class="bg-gray-500 text-white rounded-lg shadow-lg p-4 max-w-xs">
                <p>${message}</p>
            </div>
        </div>

    `


    newDiv.innerHTML=elem;

    maindiv.appendChild(newDiv);
    

     myinput.value=""


     maindiv.scrollTop= maindiv.scrollHeight;
    
    


  }






  
 function  sendMessage(message,user,type){

appendoutgoingMesaage(message,user,type);

socket.emit("message",{

    user:user,
    message:message
    
})

}


function appendincomingMesaage(message,user,type){
let maindiv=document.querySelector("#main");

const newDiv = document.createElement("div");


let elem= `
 <div class="flex flex-col items-start recieved">
            <span class="text-sm text-blue-400">@${user}</span>
            <div class="bg-gray-700 rounded-lg shadow-lg p-4 max-w-xs text-white">
                <p>${message}</p>
            </div>
        </div>
     

`


newDiv.innerHTML=elem;

maindiv.appendChild(newDiv);




 maindiv.scrollTop= maindiv.scrollHeight;




}




//r message

socket.on("rmessage",function({user,message}){
    appendincomingMesaage(message,user);
})



 


</script>
</body>
</html>
